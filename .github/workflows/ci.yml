# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['**', '!update/**', '!pr/**']
  push:
    branches: ['**', '!update/**', '!pr/**']
    tags: [v*]

env:
  PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
  SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
  SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
  PGP_SECRET: ${{ secrets.PGP_SECRET }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


concurrency:
  group: ${{ github.workflow }} @ ${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Test
    strategy:
      matrix:
        os: [ubuntu-22.04]
        scala:
          - 2.12.18
          - 2.12.19
          - 2.12.20
          - 2.12.21
          - 2.13.12
          - 2.13.13
          - 2.13.14
          - 2.13.15
          - 2.13.16
          - 2.13.17
          - 2.13.18
          - 3.3.0
          - 3.3.1
          - 3.3.3
          - 3.3.4
          - 3.3.5
          - 3.3.6
          - 3.3.7-RC1
          - 3.3.7
          - 3.4.0
          - 3.4.1
          - 3.4.2
          - 3.4.3
          - 3.5.0
          - 3.5.1
          - 3.5.2
          - 3.6.2
          - 3.6.3
          - 3.6.4
          - 3.7.0
          - 3.7.1
          - 3.7.2-RC1
          - 3.7.2-RC2
          - 3.7.2
          - 3.7.3-RC1
          - 3.7.3-RC2
          - 3.7.3
          - 3.7.4
          - 3.8.0-RC3
        java: [temurin@17]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup sbt
        uses: sbt/setup-sbt@v1

      - name: Setup Java (temurin@17)
        id: setup-java-temurin-17
        if: matrix.java == 'temurin@17'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: sbt

      - name: sbt update
        if: matrix.java == 'temurin@17' && steps.setup-java-temurin-17.outputs.cache-hit == 'false'
        run: sbt +update

      - name: Check that workflows are up to date
        run: sbt githubWorkflowCheck

      - name: Check headers and formatting
        if: matrix.java == 'temurin@17' && matrix.os == 'ubuntu-22.04'
        run: sbt '++ ${{ matrix.scala }}' headerCheckAll scalafmtCheckAll 'project /' scalafmtSbtCheck

      - name: Test
        run: sbt '++ ${{ matrix.scala }}' test

      - name: Check binary compatibility
        if: matrix.java == 'temurin@17' && matrix.os == 'ubuntu-22.04'
        run: sbt '++ ${{ matrix.scala }}' mimaReportBinaryIssues

      - name: Generate API documentation
        if: matrix.java == 'temurin@17' && matrix.os == 'ubuntu-22.04'
        run: sbt '++ ${{ matrix.scala }}' doc

      - name: Make target directories
        if: github.event_name != 'pull_request' && (startsWith(github.ref, 'refs/tags/v'))
        run: mkdir -p plugin/target project/target

      - name: Compress target directories
        if: github.event_name != 'pull_request' && (startsWith(github.ref, 'refs/tags/v'))
        run: tar cf targets.tar plugin/target project/target

      - name: Upload target directories
        if: github.event_name != 'pull_request' && (startsWith(github.ref, 'refs/tags/v'))
        uses: actions/upload-artifact@v5
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-${{ matrix.scala }}
          path: targets.tar

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (startsWith(github.ref, 'refs/tags/v'))
    strategy:
      matrix:
        os: [ubuntu-22.04]
        java: [temurin@17]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup sbt
        uses: sbt/setup-sbt@v1

      - name: Setup Java (temurin@17)
        id: setup-java-temurin-17
        if: matrix.java == 'temurin@17'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: sbt

      - name: sbt update
        if: matrix.java == 'temurin@17' && steps.setup-java-temurin-17.outputs.cache-hit == 'false'
        run: sbt +update

      - name: Download target directories (2.12.18)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.12.18

      - name: Inflate target directories (2.12.18)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.12.19)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.12.19

      - name: Inflate target directories (2.12.19)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.12.20)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.12.20

      - name: Inflate target directories (2.12.20)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.12.21)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.12.21

      - name: Inflate target directories (2.12.21)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.12)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.12

      - name: Inflate target directories (2.13.12)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.13)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.13

      - name: Inflate target directories (2.13.13)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.14)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.14

      - name: Inflate target directories (2.13.14)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.15)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.15

      - name: Inflate target directories (2.13.15)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.16)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.16

      - name: Inflate target directories (2.13.16)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.17)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.17

      - name: Inflate target directories (2.13.17)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.18)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-2.13.18

      - name: Inflate target directories (2.13.18)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.0)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.0

      - name: Inflate target directories (3.3.0)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.1

      - name: Inflate target directories (3.3.1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.3)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.3

      - name: Inflate target directories (3.3.3)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.4)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.4

      - name: Inflate target directories (3.3.4)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.5)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.5

      - name: Inflate target directories (3.3.5)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.6)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.6

      - name: Inflate target directories (3.3.6)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.7-RC1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.7-RC1

      - name: Inflate target directories (3.3.7-RC1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.3.7)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.3.7

      - name: Inflate target directories (3.3.7)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.4.0)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.4.0

      - name: Inflate target directories (3.4.0)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.4.1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.4.1

      - name: Inflate target directories (3.4.1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.4.2)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.4.2

      - name: Inflate target directories (3.4.2)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.4.3)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.4.3

      - name: Inflate target directories (3.4.3)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.5.0)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.5.0

      - name: Inflate target directories (3.5.0)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.5.1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.5.1

      - name: Inflate target directories (3.5.1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.5.2)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.5.2

      - name: Inflate target directories (3.5.2)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.6.2)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.6.2

      - name: Inflate target directories (3.6.2)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.6.3)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.6.3

      - name: Inflate target directories (3.6.3)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.6.4)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.6.4

      - name: Inflate target directories (3.6.4)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.0)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.0

      - name: Inflate target directories (3.7.0)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.1

      - name: Inflate target directories (3.7.1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.2-RC1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.2-RC1

      - name: Inflate target directories (3.7.2-RC1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.2-RC2)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.2-RC2

      - name: Inflate target directories (3.7.2-RC2)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.2)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.2

      - name: Inflate target directories (3.7.2)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.3-RC1)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.3-RC1

      - name: Inflate target directories (3.7.3-RC1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.3-RC2)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.3-RC2

      - name: Inflate target directories (3.7.3-RC2)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.3)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.3

      - name: Inflate target directories (3.7.3)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.7.4)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.7.4

      - name: Inflate target directories (3.7.4)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.8.0-RC3)
        uses: actions/download-artifact@v6
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-3.8.0-RC3

      - name: Inflate target directories (3.8.0-RC3)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Import signing key
        if: env.PGP_SECRET != '' && env.PGP_PASSPHRASE == ''
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
          PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
        run: echo $PGP_SECRET | base64 -d -i - | gpg --import

      - name: Import signing key and strip passphrase
        if: env.PGP_SECRET != '' && env.PGP_PASSPHRASE != ''
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
          PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
        run: |
          echo "$PGP_SECRET" | base64 -d -i - > /tmp/signing-key.gpg
          echo "$PGP_PASSPHRASE" | gpg --pinentry-mode loopback --passphrase-fd 0 --import /tmp/signing-key.gpg
          (echo "$PGP_PASSPHRASE"; echo; echo) | gpg --command-fd 0 --pinentry-mode loopback --change-passphrase $(gpg --list-secret-keys --with-colons 2> /dev/null | grep '^sec:' | cut --delimiter ':' --fields 5 | tail -n 1)

      - name: Publish
        env:
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
          SONATYPE_CREDENTIAL_HOST: ${{ secrets.SONATYPE_CREDENTIAL_HOST }}
        run: sbt tlCiRelease

  dependency-submission:
    name: Submit Dependencies
    if: github.event.repository.fork == false && github.event_name != 'pull_request'
    strategy:
      matrix:
        os: [ubuntu-22.04]
        java: [temurin@17]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup sbt
        uses: sbt/setup-sbt@v1

      - name: Setup Java (temurin@17)
        id: setup-java-temurin-17
        if: matrix.java == 'temurin@17'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: sbt

      - name: sbt update
        if: matrix.java == 'temurin@17' && steps.setup-java-temurin-17.outputs.cache-hit == 'false'
        run: sbt +update

      - name: Submit Dependencies
        uses: scalacenter/sbt-dependency-submission@v2
        with:
          modules-ignore: root_2.12 root_2.12 root_2.12 root_2.12 root_2.13 root_2.13 root_2.13 root_2.13 root_2.13 root_2.13 root_2.13 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 root_3 tests_2.12 tests_2.12 tests_2.12 tests_2.12 tests_2.13 tests_2.13 tests_2.13 tests_2.13 tests_2.13 tests_2.13 tests_2.13 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3 tests_3
          configs-ignore: test scala-tool scala-doc-tool test-internal
